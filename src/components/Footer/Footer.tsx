import './Footer.scss';
import { Sidenav } from '../Navigation/Sidenav';

export const Footer = () => {
    return (
        <footer className='Footer'>
            <Sidenav />   {/* it wont't to be sideNav */}
          
        </footer>
    );
}